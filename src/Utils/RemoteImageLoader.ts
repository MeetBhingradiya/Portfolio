/**
 * Returns the provided image source unchanged.
 *
 * @param src - The image source to be returned as is, which can be of any type.
 *
 * @example
 * const image = RemoteImageLoader({ src: "https://example.com/image.png" });
 */


export default function RemoteImageLoader({
    src,
}: any) {
    return src
}